(library
  (name main)
  (preprocess (pps ppx_deriving.show)))

(ocamllex OQLexer)
(menhir
  (modules OQParser)
  (flags --no-dollars --explain --dump)
  (infer true))

(env
  (dev
    (flags (:standard -w -39))))
